# Kubernetes Secret for Peak Detector - FILL IN MANUALLY!
# Create with: kubectl apply -f secret-peak-detector.yaml

apiVersion: v1
kind: Secret
metadata:
  name: peak-detector-secrets
  namespace: ailog-analyzer
type: Opaque
stringData:
  # PostgreSQL credentials
  db.user: "ailog_analyzer_user_d1"
  db.password: "y01d40Mmdys/lbDE"  # CHANGE IN PRODUCTION!
  
  # Elasticsearch credentials
  es.user: "readonly"
  es.password: "mKD7x1fCpkdBCD49qSJfuSRM"  # CHANGE IN PRODUCTION!

